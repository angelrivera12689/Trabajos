//con parametros
function saludo(pasaludar){

    let saludar = pasaludar + " Con parametros"

    return(saludar);
}

// con expresion

const saludoexp= function(pasaludo){

    let saludar = pasaludo + " Con expresion"

    return(saludar);

}